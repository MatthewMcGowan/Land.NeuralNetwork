namespace Land.NeuralNetwork.Console
{
    using System;

    class Program
    {
        static void Main(string[] args)
        {
            // Load postcodes csv into DB. Mongo?
            // Send postcode data to DB using Akka?

            // Create neural network.

            // Read the land registry csv.
            // Don't load entirety into memory, process one or a few rows at a time.
            // Encode data, pass through neural network as training data.

            // Pass x values to neural network, estimate y value.
        }
    }
}
